/* FLOAT MULTIPLICATION */
#include <stdio.h>
int main()
{
	float b, a, x;
	int y;
	printf("ENTER THE TWO FLOAT NUMBERS: ");
	scanf("%f%f",&a, &b);
	x = b * a;
	y = b * a;
	printf("Floating product is %f \n", x);
	printf("Integer product is %d", y);
	return 0;
}
